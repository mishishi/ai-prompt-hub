# 系统审计报告 — 2026-05-19
## 按 audit-prompt.md 12 维度标准

---

## P1 HIGH

### [SEC-03] `lib/auth.ts` dev 环境返回 null 导致所有受保护 API 401
- **文件**: `lib/auth.ts:7`
- **问题**: `if (!clerk) return null` > API 中 `if (!auth) return 401`，未配 `CLERK_SECRET_KEY` 时所有 POST 端点不可用。
- **修复**: 已在本次审计中修复为 `return { userId: "dev" }`。

### [COR-02] `lib/auth.ts` 返回 null 与注释语义矛盾
- **文件**: `lib/auth.ts:7`
- **问题**: 注释写 "allow through for dev" 但实际返回 null 触发 API 401。
- **修复**: 同上。

---

## P2 MEDIUM

### [SEC-04] 15 个文件含 UTF-8 BOM > 可能导致运行时解析异常
- **文件**: `lib/auth.ts`、`lib/rate-limit.ts`、`api/community/leaderboard.ts`、`src/i18n/translations.ts`、`src/components/templates/AuthorPage.tsx`、`src/components/templates/CommentsSection.tsx` 等 15 个
- **问题**: PowerShell `WriteAllText(UTF8)` 默认写入 BOM。Vercel 运行时解析 JSON/TypeScript 时可能失败。
- **修复**: 已在本次审计中全部清除。

### [API-03] GET `/api/community/[id]/comments` 错误加了认证
- **文件**: `api/community/[id]/comments.ts:7`
- **问题**: 读评论是公开操作，不应要求登录。
- **修复**: 已在本次审计中移除 GET 的 `verifyAuth`，POST 保留。

---

## P3 LOW

### [CODE-01] localStorage key 硬编码分散在 6 个文件中
- **文件**: `Dashboard.tsx`、`GeneratePage.tsx`、`TemplateDetail.tsx`、`ai.ts`、`storage.ts`、`OnboardingGuide.tsx`
- **问题**: `promptbench-tpl-feedback`、`promptbench-saved-templates` 等键在多个文件中硬编码。
- **修复**: 集中到 `src/utils/constants.ts`，统一 `STORAGE_KEYS` 对象管理。

### [ARCH-01] 3 个组件超过 500 行
- **文件**: `Dashboard.tsx (653行)`、`TemplateDetail.tsx (557行)`、`GeneratePage.tsx (517行)`
- **问题**: 单文件过大，useMemo 块过多。
- **修复**: 拆分为自定义 hook。

### [PERF-01] 社区模板列表无服务端分页 cursor
- **文件**: `api/community/index.ts:42-48`
- **问题**: 用 `limit()` 但无 offset/cursor。
- **修复**: 加 offset 参数或 cursor-based 分页。

### [UX-01] AI 生成无"使用历史"
- **文件**: `src/components/workspace/GeneratePage.tsx`
- **问题**: 用户生成完 Prompt 就丢了，无法回溯。
- **修复**: 保存到 localStorage，展示最近 5 条生成记录。

---

## 审计元数据

- 审计版本: v1
- 审计标准: audit-prompt.md
- 构建: tsc -b PASS
- 测试: 61 tests PASS
- BOM 清除: 15 文件已处理